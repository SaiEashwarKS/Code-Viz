complete pointers
complete (/remove) functions
improve getInsertY
make this.objList an object of objects with id as key instead of it being a list of objects
when there are multiple data fields in struct, there is a bug
get list of all pointers in struct, not just struct pointers
add pointers in struct to ptrObjList
when node in LL has multiple data fields, not working
add name of the struct variable in generic visualisation
y coordinates of struct variables and pointers
